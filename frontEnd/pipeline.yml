stages:
  - test

front-test:
  image: node:16-alpine
  stage: test
  script:
    - npm i npm@latest -g
    - npm i
    - npm run test
  only:
    - develop

default:
  before_script:
    - cd "$CI_PROJECT_DIR/frontEnd"